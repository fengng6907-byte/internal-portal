/** @type {import('next').NextConfig} */
const nextConfig = {
  // Python API functions are served by Vercel at /api/* via vercel.json.
  // In local dev, run Python functions via `vercel dev` (Vercel CLI).
  reactStrictMode: true,
};

module.exports = nextConfig;
